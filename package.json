{
  "name": "bytewave-landing",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "check": "node --check api/early-access.js && node --check js/main.js && node --check scripts/dev-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
